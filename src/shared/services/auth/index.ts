export { AuthService } from './AuthService';
export { hasPermission, getRolePermissions, ACTIONS, RESOURCES } from './permissionMatrix';
export type {
  IAuthService,
  Credentials,
  AuthToken,
  AuditEntry,
  AuditFilter,
  Permission,
} from './types';
